<view wx:for="{{array}}">
<view class="list">
  <image src="{{item.image}}" ></image>
  <view class="text">
  <view >{{item.name}}</view>
 <view> {{item.price}}</view>
 </view>
</view>
</view>